Test repository to build python applications

In order to consider makefile, please run:

```bash
make all
```

from the root folder, in order to consider install of requirements, format and tests, for example